name: Release Homebrew Binary

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to publish (e.g. v0.1.21)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-and-upload-binary:
    name: Build macOS binary (arm64)
    runs-on: macos-14

    env:
      RELEASE_TAG: ${{ github.event.release.tag_name || inputs.tag }}

    steps:
      - name: Validate release tag
        run: |
          if [ -z "${RELEASE_TAG}" ]; then
            echo "Error: release tag is empty."
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install . pyinstaller

      - name: Build standalone binary
        run: |
          pyinstaller --onefile --name noai-watermark src/noai_cli.py
          chmod +x dist/noai-watermark

      - name: Package release asset
        run: |
          tar -czf noai-watermark-darwin-arm64.tar.gz -C dist noai-watermark

      - name: Upload asset to GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${RELEASE_TAG}" noai-watermark-darwin-arm64.tar.gz --clobber

  update-homebrew-tap:
    name: Update Homebrew tap formula
    runs-on: ubuntu-latest
    needs: build-and-upload-binary

    env:
      RELEASE_TAG: ${{ github.event.release.tag_name || inputs.tag }}
      TAP_REPOSITORY: ${{ vars.HOMEBREW_TAP_REPOSITORY || 'mertizci/homebrew-noai-watermark' }}

    steps:
      - name: Validate release tag
        run: |
          if [ -z "${RELEASE_TAG}" ]; then
            echo "Error: release tag is empty."
            exit 1
          fi

      - name: Checkout tap repository
        uses: actions/checkout@v4
        with:
          repository: ${{ env.TAP_REPOSITORY }}
          token: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}
          path: tap

      - name: Download release asset and calculate checksum
        run: |
          URL="https://github.com/${GITHUB_REPOSITORY}/releases/download/${RELEASE_TAG}/noai-watermark-darwin-arm64.tar.gz"
          curl -fL "${URL}" -o noai-watermark-darwin-arm64.tar.gz
          SHA256="$(sha256sum noai-watermark-darwin-arm64.tar.gz | awk '{print $1}')"
          VERSION="${RELEASE_TAG#v}"

          echo "ASSET_URL=${URL}" >> "${GITHUB_ENV}"
          echo "ASSET_SHA256=${SHA256}" >> "${GITHUB_ENV}"
          echo "VERSION=${VERSION}" >> "${GITHUB_ENV}"

      - name: Render Homebrew formula
        run: |
          cat > tap/Formula/noai-watermark.rb <<EOF
          class NoaiWatermark < Formula
            desc "Remove invisible AI watermarks and manage AI image metadata"
            homepage "https://github.com/${GITHUB_REPOSITORY}"
            version "${VERSION}"
            license "MIT"

            on_macos do
              on_arm do
                url "${ASSET_URL}"
                sha256 "${ASSET_SHA256}"
              end
            end

            def install
              bin.install "noai-watermark"
            end

            test do
              assert_match "noai-watermark", shell_output("#{bin}/noai-watermark --help")
            end
          end
          EOF

      - name: Commit and push formula update
        working-directory: tap
        run: |
          if git diff --quiet -- Formula/noai-watermark.rb; then
            echo "Formula is already up to date."
            exit 0
          fi

          git add Formula/noai-watermark.rb
          git -c user.name="github-actions[bot]" \
              -c user.email="github-actions[bot]@users.noreply.github.com" \
              commit -m "chore: bump noai-watermark to ${VERSION}"
          git push
